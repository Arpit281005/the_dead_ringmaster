# Copy to .env for local development.

DATABASE_URL="file:./prisma/dev.db"

# Min 16 chars. Required in production. Dev falls back to a fixed local secret if unset.
QR_HMAC_SECRET="change-me-to-a-long-random-secret"
